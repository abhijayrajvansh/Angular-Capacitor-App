import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rajvansh.angular',
  appName: 'Angular-Capacitor-App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
